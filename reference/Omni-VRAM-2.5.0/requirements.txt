# Omni-VRAM v2.5.0 - Dependencies
# ================================
#
# Install core dependencies:
#   pip install -r requirements.txt
#
# Install with optional feature groups:
#   pip install -e ".[audio,realtime,tts,translation,grpc]"

# ---- Core Dependencies ----
numpy>=1.20.0
pydub>=0.25.1
python-dotenv>=1.0.0
soundfile>=0.10.0
requests>=2.28.0
scipy>=1.7.0

# ---- Audio Processing ----
sounddevice>=0.4.6
webrtcvad>=2.0.10

# ---- Whisper Backends ----
# faster-whisper (recommended, primary backend)
faster-whisper>=0.10.0
# OpenAI API backend (optional, requires API key)
# openai>=1.0.0
# whisper.cpp Python bindings (optional)
# whisper-cpp-python>=0.1.0

# ---- CUDA / PyTorch (required for CUDA extension) ----
# Install separately: pip install torch --index-url https://download.pytorch.org/whl/cu121
torch>=1.9.0

# ---- Web API Server (optional) ----
# fastapi>=0.104.0
# uvicorn>=0.24.0
# python-multipart>=0.0.6

# ---- Gradio Web Demo (optional) ----
# gradio>=4.0.0

# ---- Monitoring (optional) ----
# psutil>=5.9.0

# ---- Development ----
# pytest>=7.0.0
